# Nemesis Test Framework - Environment Variables Template
# Copy this file to .env and configure with your values

# =============================================================================
# TEST EXECUTION CONFIGURATION
# =============================================================================

# Test environment (e.g., dev, stage, prod)
# Used to select environment-specific configuration
TEST_ENV=dev

# Run tests in headless browser mode (true/false)
# Set to 'false' for visible browser during development
HEADLESS=true

# Enable debug mode for verbose logging (true/false)
# Set to 'true' for detailed debugging output
DEBUG=false

# Python I/O encoding (recommended: utf-8 for Persian/Farsi support)
# Ensures proper handling of UTF-8 characters in test data
PYTHONIOENCODING=utf-8

# =============================================================================
# REPORTPORTAL CONFIGURATION
# =============================================================================

# Enable ReportPortal reporting (true/false)
# Set to 'true' to send test results to ReportPortal instance
RP_ENABLED=false

# ReportPortal server endpoint URL
# Example: https://reportportal.example.com
RP_ENDPOINT=

# ReportPortal project name
# The project where test results will be reported
RP_PROJECT=

# ReportPortal API key for authentication
# Generate from ReportPortal user profile settings
# NOTE: This is a local environment - password masking not required
RP_API_KEY=

# =============================================================================
# NEMESIS INTERNAL PATHS (Auto-configured, rarely need to override)
# =============================================================================

# Execution ID for test run (auto-generated if not set)
# Used internally for tracking test execution sessions
# NEMESIS_EXECUTION_ID=

# Custom features directory path (optional)
# Override default features directory location
# NEMESIS_FEATURES_DIR=

# Custom reports output directory (optional)
# Override default reports output location
# NEMESIS_REPORTS_DIR=

# =============================================================================
# NOTES
# =============================================================================

# 1. This is a LOCAL ENVIRONMENT template
#    - No need for password masking or security hardening
#    - Suitable for development and testing purposes
#
# 2. Boolean values accept: true/false, 1/0, yes/no (case-insensitive)
#
# 3. Environment variables override configuration files (playwright.yaml, etc.)
#
# 4. For ReportPortal setup:
#    - Create a project in ReportPortal
#    - Generate an API key from user profile
#    - Configure RP_ENDPOINT, RP_PROJECT, and RP_API_KEY
#    - Set RP_ENABLED=true
#
# 5. Recommended development settings:
#    HEADLESS=false  # See browser during test development
#    DEBUG=true      # Verbose logging for troubleshooting
#    RP_ENABLED=false # Disable ReportPortal for faster local runs
#
# 6. Recommended CI/CD settings:
#    HEADLESS=true
#    DEBUG=false
#    RP_ENABLED=true
