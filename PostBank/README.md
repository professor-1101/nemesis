# PostBank Test Automation

پروژه اتوماسیون تست برای سیستم پست‌بانک با استفاده از فریمورک Nemesis، Behave و Playwright.

## 🎯 نمای کلی پروژه

این پروژه تست‌های end-to-end برای سیستم پست‌بانک را با استفاده از:
- **سناریوهای BDD**: تست‌های مبتنی بر Gherkin به زبان فارسی
- **Page Object Model**: الگوی طراحی برای تعامل با صفحات
- **Multi-Environment**: پیکربندی‌های مختلف برای محیط‌های مختلف
- **گزارش‌دهی پیشرفته**: HTML، یکپارچه‌سازی ReportPortal
- **جمع‌آوری Artifacts**: اسکرین‌شات، ویدیو، trace، لاگ‌های شبکه

## 📁 ساختار پروژه

```
PostBank/
├── conf/                    # فایل‌های پیکربندی
│   ├── behave.ini          # تنظیمات Behave
│   ├── playwright.yaml      # پیکربندی مرورگر
│   ├── reporting.yaml       # تنظیمات گزارش
│   ├── reportportal.yaml    # پیکربندی ReportPortal
│   └── logging.yaml         # تنظیمات لاگ
├── features/                # سناریوهای BDD
│   ├── environment.py      # Hook های lifecycle
│   ├── steps/              # Step definitions
│   │   └── login_steps.py
│   ├── ورود کاربر - انتقال به داشبورد/
│   └── ثبت متقاضی — ارسال برای تأیید جهت ایجاد باجه/
├── pages/                   # Page Object Model
│   ├── base_page.py        # کلاس پایه
│   ├── login_page.py       # صفحه ورود
│   ├── dashboard_page.py    # صفحه داشبورد
│   └── applicant_page.py   # صفحه ثبت متقاضی
├── exceptions/              # مدیریت exception ها
├── reports/                 # گزارش‌های اجرای تست
├── logs/                    # لاگ‌های اجرا
└── requirements.txt        # وابستگی‌های Python
```

## 🚀 شروع سریع

### پیش‌نیازها
- Python 3.9+
- فریمورک Nemesis نصب شده
- مرورگرهای Playwright نصب شده

### نصب

1. **نصب فریمورک Nemesis**
   ```bash
   cd ../Nemesis
   pip install -e .
   playwright install chromium
   ```

2. **راه‌اندازی پروژه**
   ```bash
   cd ../PostBank
   pip install -r requirements.txt
   ```

### اجرای تست‌ها

#### دستورات پایه
```bash
# اجرای تمام تست‌ها
nemesis run

# اجرای با تگ‌های خاص
nemesis run --tags @feature

# اجرای یک feature خاص
nemesis run --feature "ورود کاربر - انتقال به داشبورد"
```

#### گزینه‌های پیشرفته
```bash
# اجرای موازی
nemesis run --parallel 4

# حالت headless
nemesis run --headless

# حالت debug
nemesis run --debug --no-headless
```

## 🧪 سناریوهای تست

### تست‌های ورود کاربر
- ✅ **ورود موفق**: ورود موفق کاربر و انتقال به داشبورد
- ❌ **ورود ناموفق**: بررسی پیام‌های خطا

### تست‌های ثبت متقاضی
- ✅ **ثبت متقاضی**: ثبت اطلاعات متقاضی و ارسال برای تأیید
- ❌ **اعتبارسنجی**: بررسی فیلدهای اجباری
- ✅ **ثبت نتیجه آزمون**: ثبت نتیجه "قبول" یا "رد"

## 📊 گزارش‌ها و Artifacts

### گزارش‌های محلی
```bash
# مشاهده گزارش HTML
open reports/2025-12-20_09-00-00_abc123/report.html
```

### ساختار گزارش
```
reports/2025-12-20_09-00-00_abc123/
├── report.html              # گزارش اصلی HTML
├── screenshots/             # اسکرین‌شات‌های مراحل
├── videos/                  # ویدیوهای سناریوها
├── traces/                  # Playwright traces
├── network/                 # فایل‌های HAR و metrics
├── performance/             # metrics عملکرد
└── console/                 # لاگ‌های کنسول مرورگر
```

## ⚙️ پیکربندی

### متغیرهای محیطی
```bash
# تنظیمات پایه
TEST_ENV=dev
BROWSER_TYPE=chromium
HEADLESS=false

# Application URL
BASE_URL=http://192.168.10.141:1365/

# ReportPortal (اختیاری)
RP_ENABLED=true
RP_ENDPOINT=http://192.168.10.191:9080
RP_PROJECT=postbank
RP_API_KEY=your-api-key-here
```

## 🔧 عیب‌یابی

### مشکلات رایج

#### 1. ماژول پیدا نشد
```bash
# راه‌حل: نصب فریمورک nemesis
cd ../Nemesis
pip install -e .
```

#### 2. مرورگر پیدا نشد
```bash
# راه‌حل: نصب مرورگرهای Playwright
playwright install chromium
```

## 📚 مستندات

- **فریمورک Nemesis**: [مستندات فریمورک](../Nemesis/README.md)
- **Behave**: https://behave.readthedocs.io/
- **Playwright**: https://playwright.dev/python/
- **ReportPortal**: https://reportportal.io/docs/

---

**موفق باشید! 🎉**

